{
  "name": "@sarvinbox-ext/otp-code",
  "version": "1.0.0",
  "private": true,
  "description": "Surfaces one-time passcodes the moment they arrive, with a copy button and an expiry countdown",
  "license": "SEE LICENSE IN ../../LICENSE",
  "author": "Sarv",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "eslint src test --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@sarvinbox/core": "workspace:*",
    "@vitest/coverage-v8": "^1.2.0",
    "tsup": "^8.0.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
